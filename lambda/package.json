{
  "name": "stockmo-vehicle-import-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function to process Excel vehicle imports for StockMo",
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "zip": "zip -r ../infra/lambda.zip . -x 'node_modules/.cache/*'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "xlsx": "^0.18.5",
    "@aws-sdk/client-s3": "^3.600.0",
    "@aws-sdk/s3-request-presigner": "^3.600.0"
  }
}
